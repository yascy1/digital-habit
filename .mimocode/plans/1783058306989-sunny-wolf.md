# Check-in Perubahan Kode & Debug 404

## Status: Resolved

## Perubahan yang Ditemukan (5 file modified, 2 new files)

### File Modified:
1. **`app/dashboard/page.tsx`** — Streak Pencatatan (ganti Digital Wellness Score), trend durasi, tooltip info
2. **`app/globals.css`** — Font heading ke Poppins, body ke Geist Sans
3. **`app/input-aktivitas/page.tsx`** — Streak refresh setelah save, toast success
4. **`app/layout.tsx`** — Import font Poppins dari Google Fonts
5. **`app/riwayat/page.tsx`** — Export PDF mode kustom, filter badge, delete dengan konfirmasi, pagination angka, redesign visual

### File Baru:
6. **`lib/streak.ts`** — Modul streak dengan `getStreak()` dan `refreshStreakIfToday()`
7. **`components/ui/tooltip.tsx`** — Komponen Tooltip dari Radix UI

## Debug 404
- Penyebab: Dev server perlu di-restart (cache stuck)
- Solusi: Ctrl+C lalu `npm run dev` lagi → resolved
