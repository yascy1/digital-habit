# Plan: Restore Missing Features Across All Branches

## Current Situation

Branch `fitur-riwayat` (currently checked out) has lost many files compared to the other branches. The core issue: login, register, profil, riwayat pages, auth-check component, types, activities lib, label component, and basePath config were all deleted.

| Branch | Status |
|--------|--------|
| `fitur-landing-login` | **Most complete** - has all features + basePath ✅ |
| `fitur-dashboard` | Complete + dashboard enhancements ✅ |
| `fitur-profile` | Complete, same as landing-login ✅ |
| `fitur-input` | Complete, same as landing-login ✅ |
| `fitur-riwayat` | **BROKEN** - missing 10 files, no basePath ❌ |
| `main` | Only project init, no features |

## Files Missing from `fitur-riwayat` (need restoration)

1. `app/login/page.tsx` - Login page
2. `app/register/page.tsx` - Register page
3. `app/profil/layout.tsx` - Profile layout with sidebar
4. `app/profil/page.tsx` - Profile page with edit/stats
5. `app/riwayat/layout.tsx` - Riwayat layout with sidebar
6. `app/riwayat/page.tsx` - Activity history with table/modal
7. `components/auth-check.tsx` - Auth guard component
8. `components/ui/label.tsx` - Label UI component
9. `lib/activities.ts` - Activity CRUD helpers
10. `lib/types.ts` - TypeScript interfaces

## Files with broken content in `fitur-riwayat`

11. `next.config.ts` - Missing `basePath: "/digital-habit"`
12. `app/page.tsx` - Is default Next.js template, should be landing page
13. `app/input-aktivitas/page.tsx` - Lost form logic (duration/date/notes state, saveActivities, router)
14. `app/dashboard/page.tsx` - Minor: "Halo, Dinda!" should be "Halo, Tyas!"
15. `app/dashboard/layout.tsx` - Minor BOM character difference

## Additional: Forgot Password Feature (NEW)

From previous session history, a "lupa password" feature was being developed at `app/lupa-password/page.tsx`. This needs to be created fresh on `fitur-landing-login` and propagated to all other branches.

---

## Execution Plan (per branch)

### Step 1: Fix `fitur-landing-login` branch (primary reference)

1. Switch to `fitur-landing-login`
2. Create `app/lupa-password/page.tsx` (new forgot password page)
3. Add "Lupa Password?" link to `app/login/page.tsx`
4. Create `app/lupa-password/layout.tsx` (auth layout with sidebar)
5. Commit the forgot password feature

### Step 2: Fix `fitur-riwayat` branch (currently checked out - most broken)

1. Switch to `fitur-riwayat`
2. Restore `next.config.ts` with `basePath: "/digital-habit"`
3. Restore `app/page.tsx` from `fitur-landing-login` (landing page)
4. Restore all 10 missing files from `fitur-landing-login`:
   - `app/login/page.tsx`
   - `app/register/page.tsx`
   - `app/profil/layout.tsx`
   - `app/profil/page.tsx`
   - `app/riwayat/layout.tsx`
   - `app/riwayat/page.tsx`
   - `components/auth-check.tsx`
   - `components/ui/label.tsx`
   - `lib/activities.ts`
   - `lib/types.ts`
5. Fix `app/input-aktivitas/page.tsx` - restore full form logic (state, handlers, saveActivities)
6. Fix `app/dashboard/page.tsx` - change "Dinda" back to "Tyas"
7. Add forgot password feature (from step 1)
8. Commit

### Step 3: Fix `fitur-dashboard` branch

1. Switch to `fitur-dashboard`
2. Verify all files exist (should already be complete)
3. Add forgot password feature
4. Commit

### Step 4: Fix `fitur-profile` branch

1. Switch to `fitur-profile`
2. Verify all files exist (should already be complete)
3. Add forgot password feature
4. Commit

### Step 5: Fix `fitur-input` branch

1. Switch to `fitur-input`
2. Verify all files exist (should already be complete)
3. Add forgot password feature
4. Commit

### Step 6: Fix `main` branch

1. Switch to `main`
2. Cherry-pick or merge base features from `fitur-landing-login` to bring main up to date
3. Commit

---

## Key Details

### `app/lupa-password/page.tsx` (New Feature)
Based on prior session design:
- Uses react-hook-form + zod for validation
- Step 1: Enter email → find user in `digital-habit-users` localStorage
- Step 2: Verify identity by matching name
- Step 3: Set new password → update in localStorage
- Redirect to login after reset
- UI consistent with login/register pages (Card, Label, Input, Button)

### `app/lupa-password/layout.tsx`
- No sidebar needed (like login/register pages)
- Simple centered layout

### Login page update
- Add "Lupa Password?" link below the form, pointing to `/lupa-password`

---

## Verification

After each branch fix:
1. `npm run build` to verify no TypeScript/compilation errors
2. `npm run lint` to verify no linting issues
3. Spot-check that all routes are accessible: `/`, `/login`, `/register`, `/lupa-password`, `/dashboard`, `/input-aktivitas`, `/profil`, `/riwayat`

---

## Critical Files

| File | Action |
|------|--------|
| `next.config.ts` | Add basePath |
| `app/page.tsx` | Replace with landing page |
| `app/login/page.tsx` | Restore + add forgot password link |
| `app/register/page.tsx` | Restore from git |
| `app/profil/layout.tsx` | Restore from git |
| `app/profil/page.tsx` | Restore from git |
| `app/riwayat/layout.tsx` | Restore from git |
| `app/riwayat/page.tsx` | Restore from git |
| `app/lupa-password/page.tsx` | **NEW** - Create fresh |
| `app/lupa-password/layout.tsx` | **NEW** - Create fresh |
| `components/auth-check.tsx` | Restore from git |
| `components/ui/label.tsx` | Restore from git |
| `lib/activities.ts` | Restore from git |
| `lib/types.ts` | Restore from git |
| `app/input-aktivitas/page.tsx` | Restore full form logic |
| `app/dashboard/page.tsx` | Fix greeting text |
