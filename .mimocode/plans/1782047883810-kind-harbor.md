# Plan: Tambah basePath `/digital-habit`

## Goal
Tambah `basePath: "/digital-habit"` di `next.config.ts` agar semua route di-prefix `/digital-habit/`.

## Changes
1. **`next.config.ts`** — tambah `basePath: "/digital-habit"`
2. **`app/page.tsx`** — tidak perlu ubah (Next.js `Link` otomatis handle basePath)

## Verification
1. Buka `http://localhost:3000/digital-habit/` — landing page tampil
2. Klik "Masuk" → redirect ke `/digital-habit/login`
