# Plan: Fix avgTrendStr di Dashboard

## File: `app/dashboard/page.tsx` (line 272-276)

### Sebelum (bug):
```tsx
const avgTrendStr = period === "Harian"
  ? "Sesi yang kamu catat hari ini"
  : previousTotal === 0
    ? "Belum ada data pembanding"
    : `${diffAvg >= 0 ? "▲ Naik" : "▼ Turun"} ${formatMinutes(Math.abs(diffAvg))} dari ${periodLabel}`
```

### Sesudah (fix):
```tsx
const avgTrendStr = period === "Harian"
  ? "Sesi yang kamu catat hari ini"
  : previousTotal === 0
    ? "Belum ada data pembanding"
    : diffAvg === 0
      ? "Sama seperti period sebelumnya"
      : `${diffAvg >= 0 ? "▲ Naik" : "▼ Turun"} ${formatMinutes(Math.abs(diffAvg))} dari ${periodLabel}`
```

### Yang berubah
Tambah kondisi `diffAvg === 0` → "Sama seperti period sebelumnya" di antara `previousTotal === 0` dan kondisi naik/turun.

### Yang TIDAK berubah
- `period === "Harian"` — tetap "Sesi yang kamu catat hari ini"
- `totalTrendStr` — sudah benar, tidak disentuh
